import { Meta, Story, Canvas } from '@storybook/blocks';
import * as LoaderStories from './Loader.stories';

<Meta title="common/Loader" of={LoaderStories} />

# Loader

Loading 상태를 나타내는 컴포넌트로, NextApp 컴포넌트의 자식 컴포넌트로 딱 한 번 사용됩니다.

Recoil 전역 상태를 이용하여 렌더링 여부를 결정합니다.

<Canvas 
  of={LoaderStories.Example}
  sourceState="shown"
  story={{
        inline: false,
        height: '80vh'
    }} 
  source={{
    language: 'jsx',
    code: `() => {
        const setLoadingState = useSetRecoilState(loadingState);
        setLoadingState(true)
        return (
            <Loader />
        );
    };`
  }}
/>