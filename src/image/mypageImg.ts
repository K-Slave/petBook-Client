const STORAGE_PATH = process.env.NEXT_PUBLIC_STORAGE;
const PAGE_PATH = "mypage";

const mypageImg = {
  banner: `${STORAGE_PATH}/${PAGE_PATH}/mypage_banner_img.png`,
  defaultUserImg: `${STORAGE_PATH}/${PAGE_PATH}/mypage_default_userimg.png`,
  follow: `${STORAGE_PATH}/${PAGE_PATH}/mypage_follow_icon.png`,
};

export default mypageImg;
